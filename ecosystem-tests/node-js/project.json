{
  "description": "plain JavaScript via require() and import, node --test, tsc --checkJs over the JS",
  "requires": ["node"],
  "packageManager": "npm",
  "perNodeVersion": true,
  "minNodeVersion": 20,
  "steps": [
    ["npx", "tsc", "-p", "."],
    ["node", "--test", "cjs/test.cjs", "esm/test.mjs"]
  ]
}
