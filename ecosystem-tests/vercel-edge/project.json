{
  "description": "Vercel Edge Function: tsc (Next.js-style lib dom + bundler resolution), esbuild as `vercel dev` bundles edge functions (platform=browser, conditions edge-light), run in edge-runtime's VM via node --test",
  "requires": ["node"],
  "packageManager": "npm",
  "minNodeVersion": 20,
  "steps": [
    ["npx", "tsc", "--noEmit"],
    ["npx", "esbuild", "src/handler.ts", "--bundle", "--platform=browser", "--conditions=edge-light", "--format=iife", "--global-name=edgeFunction", "--target=es2022", "--outfile=dist/handler.js"],
    ["node", "--test", "tests/edge.test.mjs"]
  ]
}
