# pnpm's isolated layout only links what each package declares, so an undeclared
# (phantom) dependency inside the SDK fails to resolve here instead of being hoisted
node-linker=isolated
hoist=false
public-hoist-pattern[]=
# zod is an optional peer: the SDK must import and run without it
auto-install-peers=false
strict-peer-dependencies=true
