# PnP throws on any require/import of a package the importer does not declare,
# so an undeclared dependency inside the SDK fails here rather than resolving by luck
nodeLinker: pnp
# not even the SDK may fall back to this project's own dependencies
pnpFallbackMode: none
enableGlobalCache: false
enableTelemetry: false
