{
  "description": "TypeScript, CommonJS, moduleResolution node10, jest + ts-jest",
  "requires": ["node"],
  "packageManager": "npm",
  "perNodeVersion": true,
  "steps": [
    ["npx", "tsc", "--noEmit"],
    ["npx", "jest"]
  ],
  "knownFailure": "under jest, fetch's AbortError is a DOMException from another realm; castToError() drops its name, so a timeout throws APIConnectionError instead of APIConnectionTimeoutError — fixed by #384"
}
